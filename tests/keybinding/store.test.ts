import { describe, it, expect } from "vitest";

// Unit test skeleton for keybinding store
describe("Keybinding store", () => {
  it("should load default shortcuts without error", () => {
    expect(true).toBe(true);
  });
});
